<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1>Enter Daily Steps</h1>

  <%= simple_form_for @walk, :url => walker_walks_path do |f| %>
    <%= f.input :date %>
    <%= f.input :step %>
    <br />
    <%= f.submit 'Create', :class => 'btn btn-primary' %>
  <% end %>
</div>
